/*
 * Template
 */
#include "./pcon.h"

#define MAX 1000

int
main(int argc, char *argv[])
{
  int n, i, j;
  int a[MAX];
   
  scanf("%d", &n);
  for (i = 0; i < n; i++) {
    scanf("%d", &a[i]);
  }

  return (0);
}
